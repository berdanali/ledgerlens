FROM confluentinc/cp-kafka-connect:7.7.0

# Install Debezium PostgreSQL connector from Confluent Hub.
# Pin the version for reproducibility — update here when upgrading Debezium.
RUN confluent-hub install --no-prompt debezium/debezium-connector-postgresql:2.5.4
